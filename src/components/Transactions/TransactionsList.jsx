import styles from './TransactionsList.module.css';

const TransactionsList = () => {
  return (
    <div className={styles.container}>
      <p>No transactions to display (placeholder)</p>
    </div>
  );
};

export default TransactionsList;
